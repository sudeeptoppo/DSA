import java.util.ArrayList;
import java.util.Arrays;
public class snakeAndLader {
    public int Solution(int[][] board) {
        int n = board.size();

        int node = 1; //board count
        boolean flag = true;
        int [][] connections = new int[n][n];
        for(int[] arr : board) {
            Arrays.fill(arr, -1);
        }

        

    }
}
